{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Deliberately empty. Neither the splash page nor the panel (a remote http://127.0.0.1 origin, which gets no Tauri IPC anyway) calls Tauri APIs; the tray, hotkey and backend supervision all run in Rust.",
  "windows": ["main"],
  "permissions": []
}
